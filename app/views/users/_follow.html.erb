<%= form_for current_user.active_relationships.build, remote: true do |f| %>
  <div><%= f.hidden_field :followed_id, value: @user.id %></div>
  <%= f.submit t("relationship.follow"), class: "btn btn-primary" %>
<% end %>
